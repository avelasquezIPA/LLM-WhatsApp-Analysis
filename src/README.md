# src for Python scripts

This directory is intended to hold Python scripts that support the Stata analysis workflow.

Examples of scripts that might be included here are:

- Data preprocessing scripts that prepare datasets for Stata analysis.
- Automation scripts that run Stata do-files from the command line.
- Utility scripts for data validation or transformation.
- Scripts for generating reports or visualizations using Python libraries.
